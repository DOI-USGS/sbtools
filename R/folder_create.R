#' Create a folder
#' 
#' Not yet implemented
#' 
#' @param parent_id the id of the parent folder/item under which to create this one
#' @param name the folder name
#' @param session an SB session
#' 
#' @export
folder_create = function(parent_id, name, session=current_session()){
	stop("sorry, not yet implemented")
}